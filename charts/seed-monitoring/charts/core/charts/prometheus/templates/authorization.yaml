apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: frontend-ingress
  namespace: shoot--dev--mr-test1
spec:
  selector:
    matchLabels:
      app: prometheus
  action: DENY
  rules:
  - from:
    - source:
        notRequestPrincipals: ["*"]
